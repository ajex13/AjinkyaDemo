<nav class="navbar navbar-default navbar-fixed-top" >
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "HashBlog", root_path, class: "navbar-brand" %>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        <li><a href="#" >About</a></li>
        <li><a href="#" >Contact</a></li>
        <li><a href="/posts" >Blog</a></li>



      </ul>

      <ul class="nav navbar-nav navbar-right">
          <!--search -->
          <form class="navbar-form navbar-left">
            <div class="input-group">
              <%= form_tag( posts_path, :method => "get", role:"search") do %>
              <%= text_field_tag :search, params[:search], placeholder: "Search a post.." ,class:"form-control", :required => "true" %>
              <div class="input-group-btn">
                <%= button_tag(type:"submit",name: nil,class: 'btn btn-default') do   %>
                  <i class="glyphicon glyphicon-search"></i>
                <% end %>

              </div>
            </div>
            <% end %>
          </form>

          <!--search end-->
          <% if user_signed_in?  %>
          <li><%= link_to current_user.email, "#"  %></li>
          <% else %>
          <li><%= link_to "Guest", "#"  %></li>
          <% end %>
                  <% unless user_signed_in?  %>
          <li><%= link_to  new_user_registration_path do%>
                <i class="glyphicon glyphicon-user"></i> Sign Up
              <% end %>
          </li>
          <li><%= link_to  new_user_session_path do %>
                <i class="glyphicon glyphicon-log-in"></i> Login
              <% end %>
          </li>
          <% else %>
          <li><%= link_to  edit_user_registration_path  do %>
                <i class="glyphicon glyphicon-cog"></i> Settings
              <% end %>
          </li>
          <li><%= link_to  destroy_user_session_path ,method: :delete, data: {confirm: "Are you sure?"} do %>
                <i class="glyphicon glyphicon-log-out"></i> Logout
              <% end %>
          </li>
          <% end %>
      </ul>
    </div>
  </div>
</nav>
